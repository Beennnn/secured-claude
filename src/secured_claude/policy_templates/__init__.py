"""Cerbos policy template profiles bundled with the wheel.

Subdirectories ship the YAML files that `secured-claude policy template`
copies into a target dir on demand (`developer-default`, `enterprise-strict`).
"""
