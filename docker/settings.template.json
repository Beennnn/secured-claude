{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Generated by secured-claude entrypoint at first run. Hooks call the embedded /usr/local/bin/secured-claude-hook which POSTs the tool intent to the host broker (ADR-0002).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "/usr/local/bin/secured-claude-hook",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
